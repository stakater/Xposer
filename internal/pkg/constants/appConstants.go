package constants

import (
	"time"
)

const (
	KUBERNETES     = "kubernetes"
	OPENSHIFT      = "openshift"
	ALL_NAMESPACES = ""
	SERVICES       = "services"
	DOMAIN         = "Domain"
	CERT           = "-cert"
	TEN_SECONDS    = 10 * time.Second
)
